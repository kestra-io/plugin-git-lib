version=1.2.1-SNAPSHOT
kestraVersion=2.0.2
# released from its own repo, not managed by the core platform BOM, so it is bumped independently
kestraApiClientVersion=2.0.1
org.gradle.jvmargs=-XX:+ExitOnOutOfMemoryError -XX:+HeapDumpOnOutOfMemoryError
